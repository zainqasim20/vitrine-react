# Copy this file to .env.local (gitignored) and fill in real values for
# local development. In production (Vercel), set these as the project's
# Environment Variables instead -- same names, same source of truth as the
# live site.

GEMINI_API_KEY=
PEXELS_API_KEY=
OPENAI_API_KEY=
